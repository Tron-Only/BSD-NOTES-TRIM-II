
1. Write a function to return the total sum of the
opening stock for the all the items in the product
relation 1
```sql
SELECT SUM(OpeningStock) FROM products;
```
2. Write a function to return the minimum
purchased quantity in the product relation
```sql
SELECT MIN(purchasedquantity) FROM products;
```
2. Write a function to return the total number of
records in the product relation 
```

```
2. Write a function to return the maximum
purchased quantity in the product relation


Write SQL command to create a view named
selected_products to extract
productCode.productName and opening stock for
all the recordsin the product relation where the
opening stock is greater than or equal to 350

2. Write SQL command to create a view named
myfirstproduct to extract productCode and
| purchasedQuantity for the first record in the
| products relation